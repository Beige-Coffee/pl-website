I want to add a blog section to this website. Can you put the blog button to the left of the About button? It should bring the user to a page with blog articles. As of now, the only article is the following: An Approachable Deep Dive Into Lightning’s Noise Protocol

The source content is already uploaded at `client/noise_tutorial/` with this structure:

```
client/noise_tutorial/
├── tutorial_images/          (all PNG images)
├── 1.4-crypto-review.md
├── 1.5-noise-overview.md
├── 1.6-noise-setup.md
├── 1.7-noise-act-1.md
├── 1.8-noise-act-2.md
├── 1.9-noise-act-3.md
├── 1.10-sending-messages.md
├── 1.11-receiving-messages.md
├── 1.12-rotating-keys.md
└── 1.13-quiz.md
```

I want to render these as a long-form technical article within my existing site. Here's what I need:

### Page structure

Create a single route (e.g., `/noise-tutorial`) that renders the tutorial as a multi-chapter reading experience with:

1. **A left sidebar** listing all chapters, grouped into sections with visual dividers:

   **Introduction** (short intro paragraph, not from a file — generate inline)

   **Foundations**
   - Cryptographic Primitives (1.4-crypto-review.md)
   - The Noise Framework (1.5-noise-overview.md)

   **The Handshake**
   - Handshake Setup (1.6-noise-setup.md)
   - Act 1: Proving Knowledge of Identity (1.7-noise-act-1.md)
   - Act 2: Ephemeral Key Exchange (1.8-noise-act-2.md)
   - Act 3: Identity Reveal (1.9-noise-act-3.md)

   **Encrypted Messaging**
   - Sending Encrypted Messages (1.10-sending-messages.md)
   - Receiving & Decrypting Messages (1.11-receiving-messages.md)
   - Key Rotation (1.12-rotating-keys.md)

   **Quiz**
   - Test Your Knowledge (1.13-quiz.md)

2. **A main content area** that displays the selected chapter's rendered markdown.

3. **Previous/Next navigation** at the bottom of each chapter.

### Introduction content

The intro page (no source file) should contain this text:

> # Lightning's Noise Protocol: A Deep Dive
>
> The Lightning Network enables instant, low-fee Bitcoin payments through a network of bidirectional payment channels. But for a payment protocol handling real money, secure communication isn't optional — it's essential.
>
> Lightning needs **encrypted, authenticated channels** between nodes, ensuring that payment data stays private, messages can't be tampered with, and both parties can verify who they're talking to. Rather than using TLS (which relies on certificate authorities that don't fit Bitcoin's decentralized design), the Lightning developers chose the **Noise Protocol Framework** — the same framework used by WhatsApp, Slack, and WireGuard.
>
> This tutorial walks through Lightning's Noise Protocol implementation from the ground up: starting with the cryptographic building blocks, working through the three-act handshake, and finishing with encrypted message transport and key rotation. Along the way, we'll cross-reference everything against BOLT 8 (the Lightning specification for transport encryption) and real implementations.
>
> Let's get started.

### Markdown rendering requirements

- Render all markdown to HTML, including headings, bold, italic, code blocks, links, and tables.
- **`<details>` / `<summary>` dropdowns**: The markdown files contain many HTML `<details>` blocks for interactive Q&A. These MUST render and function properly (click to expand/collapse).
- **Images**: Files reference images as `./tutorial_images/filename.png`. Make sure these paths resolve correctly to `client/noise_tutorial/tutorial_images/`. The images are currently wrapped in `<p align="..."><img ...></p>` HTML tags — these should render correctly as-is, but ensure images are responsive (max-width: 100%).
- **Code blocks**: Should have syntax highlighting where possible.
- **Inline code**: Backtick code like `temp_k1`, `HKDF(ck, sk)`, etc. should render in a monospace font.

### Styling

- **Content area max-width**: ~800px for readability.
- **Dark theme preferred**: Dark background with light text, similar to mdBook's "Navy" theme.
- **Details/summary styling**: Give the dropdowns a subtle background, border, and rounded corners so they stand out as interactive elements:
  ```css
  details {
      background-color: rgba(128, 128, 128, 0.05);
      border: 1px solid rgba(128, 128, 128, 0.2);
      border-radius: 6px;
      padding: 0.5em 1em;
      margin: 1em 0;
  }
  details summary {
      cursor: pointer;
      font-weight: bold;
      padding: 0.3em 0;
  }
  details[open] summary {
      margin-bottom: 0.5em;
      border-bottom: 1px solid rgba(128, 128, 128, 0.2);
      padding-bottom: 0.5em;
  }
  ```
- **Sidebar**: Fixed/sticky on the left. Highlight the currently active chapter. Collapsible on mobile (hamburger menu or similar).
- **Typography**: Clean sans-serif for body text, monospace for code. Good line height (~1.6-1.8) for readability.

### Key requirements

1. The sidebar must show all chapters grouped with section headers and dividers.
2. Clicking a chapter in the sidebar loads that chapter's content without a full page reload.
3. All `<details>` dropdowns must work (expand/collapse on click).
4. All images must display correctly and be responsive.
5. Previous/Next buttons at the bottom of each chapter for sequential reading.
6. The page should be mobile-responsive — sidebar collapses on small screens.
7. The URL should update when navigating between chapters (e.g., `/noise-tutorial/act-1`) so chapters are linkable.